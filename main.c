#include <stdio.h>
int main() {

//    printf("hello");
    int x = 100, y = 2, z;
    int largest = 0;
    scanf("d", x);
    printf("The product of %d and %d is %d\n", x, y);
    z= x + y;
    if (x > largest)
        x == largest;
    scanf("%d", &largest);
    printf("The remainder of %d divided by %d is\n", x, y, x % y);
    if (x = y);
    printf("%d is equal to %d\n", x, y);
    printf("The sum is %d\n", x + y);
    printf("The value of x is %d\n", &x);
    return 0;


}